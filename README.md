# RoboLab

Standalone robotics RL lab: train, evaluate, transfer, and visualize runs from a dashboard.

## Phase 0

Scaffold only: empty experiments table + budget header.

```bash
cp .env.example .env   # set BUDGET_USD_CAP if you want
make dev
```

- Web: http://localhost:5173
- API: http://127.0.0.1:8000

See `docs/PHASES.md` and `docs/PHASE_0_TEST.md`.
