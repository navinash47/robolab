[project]
name = "robolab"
version = "0.1.0"
description = "RoboLab core library (stubs in Phase 0)"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["robolab"]
