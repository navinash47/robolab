# Copy to .env and fill in real values. Never commit .env.

RUNPOD_API_KEY=
WANDB_API_KEY=
WANDB_PROJECT=robolab
# Optional: org/team entity if the key is scoped to a team (e.g. umass)
# WANDB_ENTITY=
ANTHROPIC_API_KEY=

# Monthly spend cap in USD. Dashboard shows remaining = cap - month spend.
# Phase 0/1 local: month spend is 0 (no RunPod ledger yet).
BUDGET_USD_CAP=100
