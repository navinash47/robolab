[project]
name = "robolab-workspace"
version = "0.1.0"
description = "RoboLab monorepo workspace root"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[tool.uv]
package = false

[tool.uv.workspace]
members = ["robolab", "robolab_eval", "robolab_api"]
